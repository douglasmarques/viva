<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dms.viva">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.dms.viva.ui.splash.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.dms.viva.ui.photos.PhotosActivity" />

        <activity android:name="com.dms.viva.ui.photodetail.PhotoDetailActivity" />

        <activity android:name="com.dms.viva.ui.login.LoginActivity" />

        <activity
            android:name="com.dms.viva.ui.intro.IntroActivity"
            android:theme="@style/AppTheme.Intro" />


    </application>

</manifest>